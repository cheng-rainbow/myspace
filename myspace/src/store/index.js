import { createStore } from 'vuex'
import modulesUser from './user'
export default createStore({
  state: {
  },
  
  getters: {
  },

  mutations: {
  },

  actions: {
  },

  modules: {
    user : modulesUser,
  }
})
